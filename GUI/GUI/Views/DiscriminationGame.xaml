<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:GUI.ViewModels"
             mc:Ignorable="d"
             x:Class="GUI.Views.DiscriminationGame">
    <ContentPage.BindingContext>
        <local:DiscriminationGameViewModel />
    </ContentPage.BindingContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image Grid.ColumnSpan="3" Source="{Binding Image }" />
        <Button Text="Take Photo" Grid.Row="1" Grid.ColumnSpan="3" Command="{Binding UseCamera}"/>
        <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" VerticalOptions="Center" HorizontalOptions="Center" FontSize="18" Text="{Binding TalkingHeadRole}"></Label>
        <Button Text="Swap" Grid.Row="2" Grid.Column="2" Command="{Binding SwapRole}"/>
    </Grid>
</ContentPage>