<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:GUI.ViewModels"
             xmlns:ctrl="clr-namespace:GUI.Utils"
             mc:Ignorable="d"
             x:Class="GUI.MainPage">

    <ContentPage.BindingContext>
        <local:MainPageViewModel />
    </ContentPage.BindingContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height=".5*"/>
            <RowDefinition Height=".5*"/>
            <RowDefinition Height=".5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image Source="" BackgroundColor="PowderBlue" Grid.ColumnSpan="2" Grid.Row="0"/>

        <Label Grid.Row="0" Grid.ColumnSpan="2" VerticalOptions="Center" HorizontalOptions="Center" FontSize="25" Text="{Binding TalkingHeadName}"></Label>

        <Picker Grid.Row="1" Grid.Column="0"
                Title=" -- Select -- "
                ItemsSource="{Binding TalkingHeadsCollection}"
                ItemDisplayBinding="{Binding Name}"
                SelectedItem="{Binding SelectedTalkingHeadBinding}"
                ></Picker>
        <Button Grid.Row="1" Grid.Column="1" 
                Text="Load Talking Head" 
                Margin="10,0" 
                Command="{Binding ImportTalkingHead}"
                IsEnabled="{Binding CanLoadTalkingHeadBinding}"></Button>

        
        
        <Button Grid.Row="2" Grid.Column="0" 
                Text="Save Talking Head" 
                Margin="10,0" 
                Command="{Binding SaveTalkingHeadBinding}"
                IsEnabled="{Binding CanStartGameBinding}"></Button>
        <Button Grid.Row="2" Grid.Column="1" 
                Text="Start Discrimination Game" 
                Margin="10,0" 
                Command="{Binding StartDiscriminationGame}"
                IsEnabled="{Binding CanStartGameBinding}"></Button>

    </Grid>

</ContentPage>
